export class SymbolCache {
    market;
    nameToId = null;
    idToSymbol = null;
    constructor(market) {
        this.market = market;
    }
    async resolveId(symbolOrId) {
        if (typeof symbolOrId === "number")
            return symbolOrId;
        const map = await this.getNameToId();
        const id = map.get(symbolOrId.toUpperCase());
        if (id === undefined)
            throw new Error(`Symbol not found: "${symbolOrId}". Call client.refreshSymbols() to update the cache.`);
        return id;
    }
    async getSymbol(symbolOrId) {
        const id = await this.resolveId(symbolOrId);
        const map = await this.getIdToSymbol();
        const sym = map.get(id);
        if (sym === undefined)
            throw new Error(`Symbol id ${id} not found in cache.`);
        return sym;
    }
    async refresh() {
        const { symbols } = await this.market.getSymbols();
        this.nameToId = new Map(symbols.map((s) => [s.symbolName?.toUpperCase() ?? "", s.symbolId]));
        this.idToSymbol = new Map(symbols.map((s) => [s.symbolId, s]));
    }
    async getNameToId() {
        if (this.nameToId === null)
            await this.refresh();
        return this.nameToId;
    }
    async getIdToSymbol() {
        if (this.idToSymbol === null)
            await this.refresh();
        return this.idToSymbol;
    }
}
//# sourceMappingURL=symbol-cache.js.map